name: DevDebug

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          
      - name: Install dependencies
        run: |
          npm ci --legacy-peer-deps
          npm ci
        
      - name: Run lint
        run: npm run lint

      - name: Run tests
        run: npm test

      # 👇 Android build step for React Native CLI
      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Build DevDebug APK
        if: github.ref == 'refs/heads/main'
        working-directory: ./android   # ✅ corrected path
        run: ./gradlew assembleDebug   # use assembleDebug for CLI projects

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apks
          path: android/app/build/outputs/apk/**/*.apk
